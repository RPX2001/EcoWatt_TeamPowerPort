[wokwi]
version = 1
# Firmware files (generated by PlatformIO)
elf = ".pio/build/wokwi/firmware.elf"
firmware = ".pio/build/wokwi/firmware.bin"

# Optional: Custom diagram
# Uncomment to use custom diagram.json
# diagram = "diagram.json"

[[net.forward]]
# Forward port for Flask server communication
from = "esp32"
to = "host"
port = 5000
